#晋升为Medium（扶眼镜）

##模块一
1.

Q1.11010011

Q2.182

Q3.看能否被2整除 嗯 看到后面就知道是用掩码了 正确的应该是n&1 这样来判断会快 尽管一般会前者会被优化成后者 

##模块二

1.

Q1.#include <stdio.h>
```c
int main() {
    unsigned char a = 12;  // 二进制: 00001100
    unsigned char b = 25;  // 二进制: 00011001

    unsigned char res1 = a & b;
    unsigned char res2 = a | b;
    unsigned char res3 = a ^ b;
    unsigned char res4 = (a << 2) | (b >> 1);

    printf("%d %d %d %d\n", res1, res2, res3, res4);
    return 0;
}
```
A1.（1）8  二进制问00001000 换算为1*2^3 即8

（2）29   二进制为00011101 换算为1 * 2^4 + 1 * 2^3 + 1 * 2^2 + 1 即29

（3）21  二进制为00010101 即21

（4）60 即先变为（00110000）|（00001100） 最后为00111100
 
 Q2.给定一个整数x（十进制），同时指定一个位数n，确定这个整数x的二进制表示上第n位是0还是1。例如：251的二进制表示是11111011，n=3，结果返回0。

 A2.详见c
